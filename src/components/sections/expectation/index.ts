import * as InfoBox from '../../layout/infobox';
import template from './template.html?raw' assert { type: 'html' };

const name = 'expectation-section';
const subcomponents = [InfoBox];
export { name, subcomponents, template };
