import { html } from '@loopdive/web-components';
import logo from './logo.svg';

export const fragment = html`<img src="${logo}" alt="JSCraftcamp Logo" />`.content;

/* let idCounter = 1;
		const animateYears: boolean = false;
		const style: string = '';
		const currentId = idCounter++; */
